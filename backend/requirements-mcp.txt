# Requirements for MCP Server Docker image
# Minimal dependencies - no database packages (MCP server is database-free)

# ASGI server for production
uvicorn[standard]

# MCP Server Framework
fastmcp

# Web framework (Starlette transport layer)
starlette

# HTTP client for REST API calls to On-Call Health backend
httpx

# Resilience patterns
tenacity>=9.0.0
aiobreaker>=1.2.0

# Configuration and validation
pydantic
pydantic-settings
